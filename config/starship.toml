# Insert a blank line between shell prompts
add_newline = true

# Increase the default command timeout to 2 seconds
command_timeout = 2000

# Active palette — Claude light (cream + clay). Switch back to
# "catppuccin_macchiato" any time for the old dark look.
palette = "claude_light"


# Define the order and format of the information in our prompt
format = """\
[](fg:red)\
$directory\
${custom.directory_separator_not_git}\
${custom.directory_separator_git}\
$symbol($git_branch[](fg:lavender))\
$symbol( $git_commit$git_status$git_metrics$git_state)$fill$cmd_duration\
$symbol($python[ 󰨐 ](fg:overlay2))\
$username[ 󰨐 ](fg:overlay2)$hostname\
${custom.git_config_email}
$character$status"""

# Fill character (empty space) between the left and right prompt
[fill]
symbol = " "

#

[character]
#
success_symbol = "[❱❭⟩](bold rosewater)"
#success_symbol = "[](bold rosewater)"
#success_symbol = "[](bold rosewater)"
error_symbol = "[❱❭⟩](bold red bold)"
vimcmd_symbol = "[❱❭⟩](bold mauve)" # Changes to purple when in Vim Command mode

[status]
disabled = false
format = "[$symbol $common_meaning$signal_name$maybe_int]($style) "
map_symbol = true
symbol = "󱖫"
style = "italic red"

[python]
symbol = " "
style = "fg:yellow bold"
format = "[ $symbol${pyenv_prefix}($virtualenv )($version)]($style)"
disabled = false
detect_extensions = []
detect_files = []

[username]
show_always = true
style_user = "fg:flamingo bold"
format = "[$user]($style)"

[hostname]
ssh_only = false
style = "fg:peach bold"
format = "[$hostname ]($style)"

# Disable the line break between the first and second prompt lines
[line_break]
disabled = true

# Customize the format of the working directory
[directory]
truncation_length = 10
#truncation_symbol = "../"
truncate_to_repo = false
format = "[ $path ]($style)"
style = "fg:text bg:surface0"

[git_branch]
symbol = " "
format = "[ $symbol$branch(:$remote_branch) ]($style)"
style = "fg:crust bg:lavender"

[git_metrics]
disabled = false

[nodejs]
format = "via [$symbol($version )]($style)"
style = "yellow"

[package]
disabled = true # Enable to output the current working directory's package version
format = "[$symbol$version]($style) "
display_private = true

# Output the command duration if over 2 seconds
[cmd_duration]
min_time = 2_000
format = "[ 󱑎 $duration ]($style)"
style = "fg:subtext0"


# Output the current git config email address
[custom.git_config_email]
description = "Output the current git user's configured email address."
command = "git config user.email"
format = "\n[$symbol(  $output)]($style)"
# Only when inside git repository
when = "git rev-parse --is-inside-work-tree >/dev/null 2>&1"
style = "text"

# Output a styled separator right after the directory when inside a git repository.
[custom.directory_separator_git]
description = "Output a styled separator right after the directory when inside a git repository."
command = ""
format = "[](fg:surface0 bg:lavender)"
# Only when inside git repository
when = "git rev-parse --is-inside-work-tree >/dev/null 2>&1"

# Output a styled separator right after the directory when NOT inside a git repository.
[custom.directory_separator_not_git]
description = "Output a styled separator right after the directory when NOT inside a git repository."
command = ""
format = "[](fg:surface0)"
# Only when NOT inside a git repository
when = "! git rev-parse --is-inside-work-tree > /dev/null 2>&1"



# ============================================================
# Claude light palette — cream background, clay/ink accents.
# Tuned so every fg reads clearly on Ghostty's #faf9f5 cream.
# ============================================================
[palettes.claude_light]
rosewater = "#c15f3c"
flamingo  = "#b5654d"
pink      = "#b5567d"
mauve     = "#8b4789"
red       = "#b83d2e"
maroon    = "#a8443a"
peach     = "#c15f3c"
yellow    = "#a87b1f"
green     = "#5f7a2e"
teal      = "#2e8b8b"
sky       = "#3f9a9a"
sapphire  = "#2f7d8a"
blue      = "#2f6cc1"
lavender  = "#cdbfe8"
text      = "#3d3d3a"
subtext1  = "#55554f"
subtext0  = "#6b6b66"
overlay2  = "#8a8a85"
overlay1  = "#9a9a92"
overlay0  = "#aaaaa0"
surface2  = "#ddd6c7"
surface1  = "#e4dfd2"
surface0  = "#ece7d8"
base      = "#faf9f5"
mantle    = "#f2efe6"
crust     = "#2b2b28"


# Original dark palette kept for easy switch-back.
[palettes.catppuccin_macchiato]
rosewater = "#f4dbd6"
flamingo = "#f0c6c6"
pink = "#f5bde6"
mauve = "#c6a0f6"
red = "#ed8796"
maroon = "#ee99a0"
peach = "#f5a97f"
yellow = "#eed49f"
green = "#a6da95"
teal = "#8bd5ca"
sky = "#91d7e3"
sapphire = "#7dc4e4"
blue = "#8aadf4"
lavender = "#b7bdf8"
text = "#cad3f5"
subtext1 = "#b8c0e0"
subtext0 = "#a5adcb"
overlay2 = "#939ab7"
overlay1 = "#8087a2"
overlay0 = "#6e738d"
surface2 = "#5b6078"
surface1 = "#494d64"
surface0 = "#363a4f"
base = "#24273a"
mantle = "#1e2030"
crust = "#181926"
